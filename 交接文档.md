# 寻亲风云录 — 开发交接文档

> 更新时间：2026-07-27

## 项目概况

纯前端文字 RPG 游戏，无构建工具、无框架依赖，原生 JS + CSS 实现。
技术栈：Vanilla JS（class 语法）、CSS（CSS 变量 + Grid/Flexbox）、LocalStorage 存档。
运行方式：任意静态服务器打开 `index.html` 即可。

## 文件结构

```
myRPG-fixed/
├── index.html          # 入口，移动端 meta 标签已配置
├── app.js              # GameApp 主类：初始化、底部导航、顶栏渲染、Tab 切换
├── css/
│   └── style.css       # 全部样式，移动端优先，480px 最大宽度
├── core/
│   ├── data.js         # DATA 全局对象：属性/职业/稀有度/词条池/世界地图/守门员
│   ├── state.js        # createDefaultState() / SaveManager / StateUtils
│   └── utils.js        # Utils.uuid() 等工具函数
├── systems/
│   ├── combat.js       # CombatEngine 战斗引擎（回合制、速度排序、伤害计算）
│   ├── equipment.js    # EquipmentSystem 装备锻造/附魔/镶嵌
│   ├── inventory.js    # InventorySystem 背包/仓库/容量管理
│   ├── npc.js          # NPCSystem NPC 对话/任务/守门人
│   └── scene.js        # SceneManager 场景管理/敌人数据/战斗触发
└── ui/
    └── renderer.js     # UIRenderer 所有 UI 渲染：创建/场景/战斗/背包/角色/设置/弹窗
```

## 本轮完成的工作

### 移动端适配（全部重写）

- **底部导航栏**：场景 / 背包 / 角色 / 设置 四个 Tab，固定底部，毛玻璃背景
- **顶部状态栏**：三行布局（名字+等级+位置 / HP+MP 血条 / 金币），sticky 吸顶
- **场景界面**：卡片式布局，NPC 按钮网格，出口按钮 2 列网格，危险出口红色边框提示
- **角色面板**：基础信息卡 + 六维属性 2x2 网格 + 装备槽位列表 + 随从卡片
- **设置面板**：自动保存开关、战斗模式显示、手动保存/导出/删除存档
- **NPC 交互**：底部弹出式面板（slideUp 动画），替代原来居中弹窗
- **战斗 UI**：竖屏适配，战斗按钮固定在导航栏上方，日志区域限高滚动
- **Toast 提示**：居中淡入淡出，用于保存成功等反馈
- **index.html**：添加 `viewport-fit=cover`、`apple-mobile-web-app-capable`、`theme-color` 等移动端 meta

### Bug 修复（前轮 + 本轮）

- 背包容量 `maxCapacity` 从 60 改为 100，对齐 GDD
- 补充 `DATA.inventory.stackLimits` 在 inventory.js 中的引用
- 装备系统添加 `typeToSlot` 映射，防止装备装错槽位
- 敌人 ID 改用 `Utils.uuid()` 避免冲突
- 等级上限随守门人击败进度正确递增
- 防御 buff 在战斗结束后正确重置
- 战斗日志默认隐藏（非战斗时不显示）
- 进入游戏时清空创建界面 DOM
- 背包关闭按钮改用事件绑定（去掉 inline onclick）
- 职业选择按钮改用 3 列 Grid 等宽布局
- 设置面板日期格式化（ISO → MM-DD HH:mm）
- `showPanel` 使用 CSS 定义的底部弹出样式，去掉 JS 内联样式

## 已知问题 / 待处理

1. **NPC 系统**：`NPCSystem.getNPCsForScene` 和 `handleAction` 已在 scene 渲染中做了 `typeof` 安全检测，但 `npc.js` 内部的任务系统、对话树尚未完整实现，目前是占位逻辑
2. **技能系统**：战斗中"技能"按钮直接走 attack 逻辑，职业分支技能（战士/游侠/法师各等级技能）尚未接入
3. **属性点分配**：角色面板显示了可分配点数，但点击属性卡片加点的交互尚未实现
4. **世界地图扩展**：目前只有灰烟村区域（9 个场景），灰烬山脉及之后的区域场景未创建
5. **物品掉落**：战斗胜利后的经验/金币/物品掉落奖励逻辑未完整实现
6. **存档导出**：`SaveManager.export` 和 `SaveManager.delete` 在 `state.js` 中可能只是占位

## 下一步开发优先级建议

1. **技能系统接入** — 战斗中技能按钮能根据职业/等级显示可用技能并释放
2. **物品掉落与拾取** — 战斗胜利后弹出奖励面板，物品进背包
3. **属性点分配** — 角色面板点击属性加点
4. **NPC 对话与任务完善** — 对话树、任务接取/提交
5. **扩展世界场景** — 灰烬山脉、灰烬矿场等后续区域
6. **商店系统** — 集市/铁匠铺/裁缝铺的买卖功能

## 架构要点

- 所有全局状态通过 `window.gameApp.state` 访问
- 场景切换通过 `CustomEvent('scene-change')` 通信
- 战斗通过 `CustomEvent('combat-start'/'combat-update'/'combat-end'/'combat-player-turn')` 通信
- UI 渲染全部在 `UIRenderer` 中，通过 `app.js` 的 Tab 切换控制显示/隐藏
- CSS 使用 `env(safe-area-inset-bottom)` 适配 iPhone 底部安全区
- 战斗中底部导航锁定为场景 Tab，防止误切
